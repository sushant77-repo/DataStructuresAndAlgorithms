Given a string, return the length of longest repeating subsequence.

E.g. s = “AABEDCBD”

Ans → 3 → Longest repeating subsequence is “ABD” → we can see ABD occurs twice with each character occurring twice separately