Given two strings, return the length of shortest common supersequence.

Common supersequence is that string that contains both strings as subsequence.

E.g. s1 = “AGGTAB”

s2 = “GXTXAYB”

Ans → 9 → “AGGXTXAYB”