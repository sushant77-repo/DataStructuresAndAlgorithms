Given a string which is an expression consisting of values “T”, “F”, and operators “&”, “|”, “^” (XOR), we need to return total number of ways the expression can be evaluated as true.

e.g. s = “T^F&T”

Ans → 2 → (T^F)&T and T^(F&T)